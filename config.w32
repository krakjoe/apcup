// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("apcup", "for apcup support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("apcup", "enable apcup support", "no");

if (PHP_APCUP != "no") {
	EXTENSION("apcup", "apcup.c");
}

